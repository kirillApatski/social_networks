import React from 'react';
import {navLinks} from "../../stylesComponent/Sidebar/navLinksData";

export const NavLink = () => {
    return (
        <>
            {
                navLinks.map(({id, label, link}) => {

                })
            }
        </>
    );
};

